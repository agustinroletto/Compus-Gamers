MAP
metodo de arrays. 


//valoracion creo que se deberia dejar de usar jsbin, porque no soporta es6 y me parece que entorpece mas de lo que ayuda. si vamos a usar vsc en el mundo laboral, porque no hacer prractica en este? y mas aun cuando hay un repo donde el profe con dos clicks ya puede subir todo y nosotros en vivo ver lo que va haciendo en nuestra pc en vez de pasar capturas.

perder tiempo en ajax cuand ose podria dedicar a otra cosa que enriquezca mas al curso.  y alvaro es insoportable, en vez de aportar destruye. 

No se usa mas el for para recorrer arrays. Map recorre el array y entre () se le pasa una función que haga lo que queremos!


var users = [
  { id: 1, nombre: "Paolo Sanchez", color: "Azul" },
  { id: 2, nombre: "Marcela Martinez", color: "Naranja" },
  { id: 3, nombre: "Jimena Perez", color: "Verde" },
  { id: 4, nombre: "Francesca Romagnoli", color: "Rojo" }
];

// for(var i = 0; i < users.length(), i++) {
// codigo
// }

// Map (Helper de Array, metodo de Array)

/*
var names = users.map(function (user) {
  return `El nombre del usuario es ${user.nombre} y su color
  favorito es el ${user.color}`;
});

console.log(names);

let user = {
  name: "John",
  age: 30
};

console.log(Object.keys(user))
console.log(Object.values(user))
*/


esto es lo mismo pero con arrow function
const colors = users.map((user, idx, array) => {
  return `${user.color} + ${idx} + ${array}`;
});

console.log(colors);