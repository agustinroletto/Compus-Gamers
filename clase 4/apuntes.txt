Lo de abajo es lo mismo que la forma que viene ya hecha en app.js pero hecho con clases.
Antes tenia una ventaja esto sobre el state del componente, pero ahora esto hace poco ya se puede usar con function:

class app extends react.components {
    render () esto es como que devuelve una funcion {
        aca va todo los de jsx
        las clases dentro de los elementos no se escriben class, sino className =""
    }
}

Cada componente podria tener su propia carpeta dentro de components. Seria una buena practica. 